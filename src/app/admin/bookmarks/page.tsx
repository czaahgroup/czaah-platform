// Same page as /dashboard/bookmarks, rendered here so it stays inside the
// admin shell (src/app/admin/layout.tsx) instead of bouncing staff into
// the separate member portal layout.
export { default } from '@/app/dashboard/bookmarks/page'
